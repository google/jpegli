#!/usr/bin/make -f

include /usr/share/dpkg/pkg-info.mk

%:
	dh $@ --buildsystem=cmake

override_dh_auto_configure:
	# TODO(deymo): Remove the DCMAKE_BUILD_TYPE once builds without NDEBUG
	# are as useful as Release builds.
        # TODO(szabadka) Re-enable jpegli after tests are fixed on Ubuntu 20.04,
        # and debian:buster
	dh_auto_configure -- \
	  -DJPEGLI_VERSION=$(DEB_VERSION) \
	  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
	  -DJPEGLI_FORCE_SYSTEM_GTEST=ON \
	  -DJPEGLI_FORCE_SYSTEM_HWY=ON \
	  -DJPEGLI_ENABLE_JPEGLI=OFF \
	  -DJPEGLI_ENABLE_JPEGLI_LIBJPEG=OFF
